name: CI-CD Pipeline
# Trigger condition for pipeline
on:
  push:
    branches: [ "master" ]
    
permissions:
  contents: write
jobs:
  compile:
    # It refers to the shared runner VM provided by GitHub
    runs-on: ubuntu-latest

    steps:
    # On runner, it creates a local copy of the repo and checkout the code
    - uses: actions/checkout@v4
    - name: Set up JDK 17
    # Here, set-up env for JDK
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn compile #mvn -B package --file pom.xml

  security-check:
    # It refers to the shared runner VM provided by GitHub
    runs-on: ubuntu-latest
    needs: compile
    
    steps:
    # On runner, it creates a local copy of the repo and checkout the code
    - uses: actions/checkout@v4
    - name: Trivy Fs Scan
      run: |
        sudo apt-get install wget gnupg
        wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | gpg --dearmor | sudo tee /usr/share/keyrings/trivy.gpg > /dev/null
        echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb generic main" | sudo tee -a /etc/apt/sources.list.d/trivy.list
        sudo apt-get update
        sudo apt-get install trivy
        
    # Trivy FileSystem Scan
    - name: Trivy Fs Scan
      run: trivy fs --format table -o fs-report.json .

    - name: gitleaks Installation
      run: sudo apt install gitleaks -y

    - name: Gitleaks Code Scan
      run: gitleaks detect source . -r gitleaks-report.json -f json
      
